# Light stack — runs the custom OpenCV pipeline, featurization, evaluation,
# the Streamlit live demo, and the Gradio demo. CPU-only, fits free hosts.
numpy>=1.24
scipy>=1.10
opencv-python-headless>=4.8
scikit-image>=0.22
pandas>=2.0
matplotlib>=3.7
tifffile>=2024.1
imagecodecs>=2024.1
streamlit>=1.36
gradio>=4.0

# The Cellpose-SAM foundation-model step needs torch + cellpose (heavy).
# Install those separately (they are too large for free hosting tiers):
#     pip install -r requirements-cellpose.txt
